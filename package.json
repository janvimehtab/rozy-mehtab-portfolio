{
  "name": "rozy-mehtab-portfolio-booking-engine",
  "version": "1.0.0",
  "description": "Production MERN Career Guidance & Booking Engine for Rozy Mehtab",
  "scripts": {
    "build": "npm --prefix client install && npm --prefix client run build && npm --prefix server install",
    "build:client": "npm --prefix client run build",
    "start": "node server/server.js",
    "start:server": "npm --prefix server start",
    "dev": "concurrently -n \"SERVER,CLIENT\" -c \"magenta,cyan\" \"npm --prefix server run dev\" \"npm --prefix client run dev\"",
    "dev:server": "npm --prefix server run dev",
    "dev:client": "npm --prefix client run dev"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "keywords": [
    "mern",
    "career-guidance",
    "booking-engine",
    "google-calendar",
    "punjabi-university"
  ],
  "author": "Antigravity",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^9.1.2"
  },
  "dependencies": {
    "cors": "^2.8.6"
  }
}
